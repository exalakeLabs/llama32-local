# Install with ./install.zsh so backend-specific PyTorch wheel indexes are
# applied consistently for CUDA/ROCm/MPS.
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0
transformers>=4.55.0
tiktoken>=0.7.0
kernels>=0.9.0
datasets>=2.16.0
peft>=0.11.0
trl>=0.8.0
accelerate>=0.30.0
sentence-transformers>=2.6.0
# kernels 0.15+ requires LayerRepository revision/version metadata that some
# Transformers import paths do not pass yet.
kernels>=0.12.0,<0.15.0
kernels-data>=0.14.0,<0.15.0
faiss-cpu>=1.7.4
numpy>=1.24.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
requests>=2.31.0
pypdf>=4.0.0
cryptography>=41.0.0
truststore>=0.8.0
sentencepiece
evaluate

# Optional CUDA-only acceleration. Install after torch in a compatible CUDA env:
#   pip install flash-attn --no-build-isolation
# Keep it out of the base requirements because its build step imports torch.
